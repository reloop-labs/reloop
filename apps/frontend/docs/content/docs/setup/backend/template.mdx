---
title: Template
icon: layout
description: Set up, configure, and run the Reloop template microservice locally.
---

<Callout type="info" title="Complete Base Setup First" variant="note">
  Ensure you've finished the [Setup](/docs/setup) — before proceeding.
</Callout>


## Service Overview

Email template CRUD, visual builder storage, and HTML compilation for outbound sends.

| Property | Value |
| :--- | :--- |
| **Directory** | `apps/backend/template` |
| **Port** | `8019` |
| **Local URL** | [https://local.reloop.sh/api/template](https://local.reloop.sh/api/template) |
| **Swagger UI** | [https://local.reloop.sh/api/template/openapi](https://local.reloop.sh/api/template/openapi) |
| **Stack** | `ElysiaJS` `PostgreSQL` `Redis` `NATS` |


## Quick Start

<Callout icon="union" variant="info">
Run from the monorepo root:
</Callout>

```bash
bun be:template:dev
```

The service starts with hot reloading on port `8019`.


## Environment Configuration

Variables live in `apps/backend/template/.env`. Running `bun env:setup` from the root auto-populates this file.

```bash
PG_URL=postgresql://reloop:reloop123@localhost:5432/reloop
REDIS_URL=redis://:reloop123@localhost:6379
PORT=8019
BASE_URL="https://local.reloop.sh"
NATS_URL=nats://localhost:4222
NODE_ENV=development
```

### Reference Table

| Variable | Required | Default |
| :--- | :--- | :--- |
| `PG_URL` | **YES** | `postgresql://...` |
| `REDIS_URL` | **YES** | `redis://...` |
| `PORT` | **YES** | `8019` |
| `BASE_URL` | **YES** | `https://local.reloop.sh` |
| `NATS_URL` | **YES** | `nats://localhost:4222` |


## Development Commands

| Command | Description |
| :--- | :--- |
| `bun be:template:dev` | Start dev server with hot reloading |
| `bun run --filter=template build` | Compile production bundle |
| `bun run --filter=template start` | Run compiled production build |
| `bun run --filter=template check-types` | TypeScript type-check |


## Architecture

| Layer | Detail |
| :--- | :--- |
| **Builder** | Stores drag-and-drop block JSON from the dashboard editor. |
| **Compilation** | Renders HTML with merge tags for the [mail](/setup/backend/mail) service. |
| **Schema** | `template` and revisions in `packages/db/src/schema/template.ts`. |
