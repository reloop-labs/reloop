---
title: Send emails with Nodemailer
sidebarTitle: Nodemailer
description: Learn how to configure Nodemailer to use Reloop's SMTP relay.
---

<Callout icon="sparkles">
  Use this pre-built prompt to get started faster.
  <PromptActions prompt="Can you show me how to configure Nodemailer to send emails through Reloop's SMTP relay?" />
</Callout>

## 1. Install

Install Nodemailer in your project:

```bash
npm install nodemailer
```

## 2. Configuration

Set up the Nodemailer transport using Reloop's SMTP settings:

```typescript
import nodemailer from 'nodemailer';

const transporter = nodemailer.createTransport({
  host: 'smtp.reloop.sh',
  port: 587,
  secure: false, // true for 465, false for other ports
  auth: {
    user: 'reloop',
    pass: process.env.RELOOP_API_KEY,
  },
});

async function main() {
  const info = await transporter.sendMail({
    from: 'Acme <onboarding@reloop.dev>',
    to: 'delivered@reloop.dev',
    subject: 'Hello from Nodemailer',
    text: 'It works!',
    html: '<strong>It works!</strong>',
  });

  console.log('Message sent: %s', info.messageId);
}

main().catch(console.error);
```
