---
title: Send emails with Node.js
sidebarTitle: Introduction
description: Learn how to send your first email using the Reloop Node.js SDK.
---

<Callout icon="sparkles">
  Use this pre-built prompt to get started faster.
  <div className="flex gap-2 mt-4">
    <button className="bg-black text-white px-3 py-1.5 rounded-md text-sm font-medium hover:bg-black/90 transition-colors">Copy prompt</button>
    <button className="bg-white border border-gray-200 text-gray-700 px-3 py-1.5 rounded-md text-sm font-medium hover:bg-gray-50 transition-colors flex items-center gap-2">
      <SimpleIcon name="siCursor" className="w-3 h-3" />
      Open in Cursor
    </button>
  </div>
</Callout>

## Prerequisites

To get the most out of this guide, you'll need to:

* [Create an API key](/learn/api-keys)
* [Verify your domain](/learn/domain)

## 1. Install

Install the Reloop Node.js SDK using your preferred package manager:

<Tabs items={['npm', 'yarn', 'pnpm', 'bun']}>
  <Tab value="npm">
    ```bash
    npm install @reloop/node
    ```
  </Tab>
  <Tab value="yarn">
    ```bash
    yarn add @reloop/node
    ```
  </Tab>
  <Tab value="pnpm">
    ```bash
    pnpm add @reloop/node
    ```
  </Tab>
  <Tab value="bun">
    ```bash
    bun add @reloop/node
    ```
  </Tab>
</Tabs>

## 2. Send email using HTML

Create a file named `index.ts` (or `index.js`) and add the following code:

```typescript
import { Reloop } from '@reloop/node';

const reloop = new Reloop('re_123456789');

(async function () {
  const { data, error } = await reloop.emails.send({
    from: 'Acme <onboarding@reloop.dev>',
    to: ['delivered@reloop.dev'],
    subject: 'Hello World',
    html: '<strong>It works!</strong>',
  });

  if (error) {
    return console.error({ error });
  }

  console.log({ data });
})();
```

## 3. Try it yourself

Run the script to send your first email:

```bash
npx ts-node index.ts
```

<Card title="API Reference" icon="siReadthedocs" href="/api-reference">
  Check out the full API reference to learn more about all available methods.
</Card>
