---
title: Contacts
description: Learn what Reloop contacts are and how to add, search, edit, unsubscribe, and delete contacts from the dashboard or API.
faq:
  - question: What is a Reloop contact?
    answer: A Reloop contact is one person record keyed by a unique email address in your organization. Subscription status, custom properties, groups, and channel preferences all live on that same record.
  - question: What happens if I add a duplicate email?
    answer: Email is unique per organization. Creating a contact with an email that already exists returns an error — update the existing contact instead of creating another one.
  - question: What is the difference between unsubscribed and blocked?
    answer: Unsubscribed means the contact opted out of your list. Blocked usually comes from deliverability issues such as a hard bounce or spam complaint, with a suppression reason on the contact.
  - question: Can I export my contacts?
    answer: Yes. From the contacts list in the dashboard, use Export CSV to download email, status, and created-at for the current view.
  - question: How do I manage contacts with code?
    answer: Use the Reloop SDK or REST API to create, list, get, update, and delete contacts. The same operations are available in the dashboard under Contacts.
---

## What is a Reloop contact?

A **contact** is one person in your organization, identified by a unique email (`con_…`). Subscription status, [properties](/docs/learn/contacts/properties), [groups](/docs/learn/contacts/groups), and [channels](/docs/learn/contacts/channels) stay on that record — so consent and traits stay consistent wherever you send.

Open the UI: [Contacts](https://reloop.sh/dashboard/contacts)

---

## Why use contacts instead of raw email lists?

- One email → one identity (no duplicate profiles across campaigns)
- Status and suppression travel with the person, not a single send
- Attach traits, segments, and per-topic preferences without juggling spreadsheets
- Same record from the **dashboard** or with **code**

---

## How do I add a contact?

<Tabs>
  <Tab title="Desktop" icon="monitor">
    ![Add Contact in the dashboard](/docs/images/docs/contacts/dashboard-tabs.png)

    1. Go to [Contacts](https://reloop.sh/dashboard/contacts)
    2. **Add Contact** (or `⌘/Ctrl+A`)
    3. Enter one or more emails — optional groups
    4. Save
  </Tab>
  <Tab title="Code" icon="code">
    <ContactsCodeSamples id="contacts.create" />
  </Tab>
</Tabs>

---

## How do I find or search contacts?

Search by email, filter by status, export CSV. Click a row for detail — properties, groups, channels, and activity.

<Tabs>
  <Tab title="Desktop" icon="monitor">
    ![Search and filter contacts](/docs/images/docs/contacts/list-view.png)

    1. Open [Contacts](https://reloop.sh/dashboard/contacts)
    2. Use **Search by email** or filter **All / Subscribed / Unsubscribed**
    3. Click a row to open detail — or **Export CSV** for an offline copy
  </Tab>
  <Tab title="Code" icon="code">
    <ContactsCodeSamples id="contacts.list" />

    <ContactsCodeSamples id="contacts.get" />
  </Tab>
</Tabs>

---

## How do I edit or unsubscribe a contact?

Change name, status, or properties — row menu or API.

| Action | What happens |
|--------|----------------|
| **Edit** | Update name or properties |
| **Subscribe / Unsubscribe** | Toggle list status |
| **View Details** | Full profile, groups, channels, activity |

Status is `subscribed`, `unsubscribed`, or `blocked` (often from deliverability).

<Tabs>
  <Tab title="Desktop" icon="monitor">
    ![Contact row actions](/docs/images/docs/contacts/list-view.png)

    1. Open [Contacts](https://reloop.sh/dashboard/contacts)
    2. Row **···** menu (or open the contact)
    3. Choose **Edit**, **Subscribe**, or **Unsubscribe**
  </Tab>
  <Tab title="Code" icon="code">
    <ContactsCodeSamples id="contacts.update" />
  </Tab>
</Tabs>

---

## How do I delete a contact?

<Tabs>
  <Tab title="Desktop" icon="monitor">
    ![Delete a contact from the list](/docs/images/docs/contacts/list-view.png)

    1. Open [Contacts](https://reloop.sh/dashboard/contacts)
    2. Row **···** → **Delete**
    3. Confirm
  </Tab>
  <Tab title="Code" icon="code">
    <ContactsCodeSamples id="contacts.delete" />
  </Tab>
</Tabs>

---

## FAQ

### What is a Reloop contact?

A Reloop contact is one person record keyed by a unique email address in your organization. Subscription status, custom properties, groups, and channel preferences all live on that same record.

### What happens if I add a duplicate email?

Email is unique per organization. Creating a contact with an email that already exists returns an error — [update](#how-do-i-edit-or-unsubscribe-a-contact) the existing contact instead of creating another one.

### What is the difference between unsubscribed and blocked?

**Unsubscribed** means the contact opted out of your list. **Blocked** usually comes from deliverability issues such as a hard bounce or spam complaint, with a suppression reason on the contact.

### Can I export my contacts?

Yes. From the contacts list in the dashboard, use **Export CSV** to download email, status, and created-at for the current view.

### How do I manage contacts with code?

Use the Reloop SDK or REST API to create, list, get, update, and delete contacts — same operations as the dashboard. Jump to the [API reference](#api-reference) below.

---

## Related

<CardGroup cols={3}>
  <Card icon="sliders" title="Properties" href="/docs/learn/contacts/properties">
    Custom string and number fields.
  </Card>
  <Card icon="users" title="Groups" href="/docs/learn/contacts/groups">
    Lists for targeted sends.
  </Card>
  <Card icon="tower-broadcast" title="Channels" href="/docs/learn/contacts/channels">
    Per-topic subscription preferences.
  </Card>
</CardGroup>

---

## API reference

<CardGroup cols={2}>
  <Card icon="plus" title="Create" href="/api/contacts/post-api-contacts-create#contacts">
    New contact.
  </Card>
  <Card icon="list" title="List" href="/api/contacts/get-api-contacts-list#contacts">
    Paginated list.
  </Card>
  <Card icon="eye" title="Retrieve" href="/api/contacts/get-api-contacts-retrieve-by-contact_id#contacts">
    By ID.
  </Card>
  <Card icon="pen" title="Update" href="/api/contacts/patch-api-contacts-by-contact_id#contacts">
    Name, status, properties.
  </Card>
  <Card icon="trash" title="Delete" href="/api/contacts/delete-api-contacts-by-contact_id#contacts">
    Remove contact.
  </Card>
</CardGroup>

[Full Contacts API](/docs/api/contacts)
