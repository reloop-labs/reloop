import { createMediaAdapters } from "fumadocs-openapi/media";

export const mediaAdapters = createMediaAdapters({
  components: {
    // You can add custom components here if needed in the future
  },
});
