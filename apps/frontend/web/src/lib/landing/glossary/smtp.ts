import type { GlossaryTermDefinition } from "../types";

export const term: GlossaryTermDefinition = {
	slug: "smtp",
	title: "SMTP",
	description:
		"Simple Mail Transfer Protocol—the standard protocol for sending email between servers.",
	keywords: ["SMTP", "what is SMTP", "SMTP relay"],
	body: "Reloop offers a managed SMTP relay with TLS and authentication—use it from Laravel, Django, or any app that speaks SMTP.",
	relatedFeatureHref: "/features/smtp",
};
