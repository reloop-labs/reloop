import {
	BlueprintCta,
	type BlueprintCtaProps,
} from "@reloop/web/components/landing/blueprint-cta";

export default function CTA(props: BlueprintCtaProps) {
	return <BlueprintCta {...props} />;
}
